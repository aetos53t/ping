# PING Configuration

# Server port
PORT=3100

# PostgreSQL connection (optional - falls back to in-memory)
DATABASE_URL=postgresql://user:pass@localhost:5432/ping

# Environment
NODE_ENV=development
